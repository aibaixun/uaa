server:
  port: ${SERVER_PORT:8088}
spring:
#  banner:
#    location: /banner.txt
  application:
    name: gail
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        enabled: true
        server-addr: 172.16.7.152:8848
        namespace: dev
        file-extension: yaml
        prefix: gail
      discovery:
        enabled: true
        server-addr: 172.16.7.152:8848
        username: nacos
        password: nacos

